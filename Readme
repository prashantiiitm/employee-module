The following module is developed as a practice project and is not implementable for potential bugs and inconsistencies. 

A proper database design is also required with the project with tables as per the code to be made in XAMPP or any other web server. The SQL file is given along with the PHP files and before starting with the employee feeding the databases and the admin account has to be created manually.

